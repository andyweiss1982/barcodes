<%= simple_form_for(@scan, html: { novalidate: false }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :image, as: :file, required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Scan" %>
  </div>
<% end %>